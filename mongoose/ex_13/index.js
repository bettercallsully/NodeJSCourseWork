
const express = require('mongoose')
const app = express()
const port = 3000
const url = "mongoose"
const router = mongoose.Router();

var mongoose = 'mongoose://my_database';
mongoose.connect(social, {useNewUrlParser: true});

var mongoose = mongoose.connection

app.get('/users:/id' (req, res) =>
UserModel.findOne({}).exect({err, user}) =>
    res.send('user:')
})


{
    "name": "mongooose",
  
    "version": "1.0.0",
    "description": "",
    "main": "mongodb.js",
    "scripts": {
      "start": "Server on port 3000"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
      "express": "^4.17.1"
    }
  }
  